# Backend server
PORT=3000

# SMTP settings (Yahoo)
# Replace with your own values and DO NOT commit real secrets.
MAIL_HOST=smtp.mail.yahoo.com
MAIL_PORT=465
MAIL_SECURE=true
MAIL_USER=miguelaude@ymail.com
MAIL_PASS=your_app_password_here

# Destination address that will receive contact messages
MAIL_TO=destino@dominio.com

# EmailJS (frontend)
# These are used if you prefer sending via EmailJS instead of SMTP.
# Public key is not a secret but keep service/template IDs configurable.
VITE_EMAILJS_PUBLIC_KEY=YOUR_PUBLIC_KEY
VITE_EMAILJS_SERVICE_ID=service_0g684ya
VITE_EMAILJS_TEMPLATE_ID=template_jm1hl2w
VITE_EMAILJS_DISABLE=false