name: Deploy to Hostinger (public_html)

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: hostinger-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    if: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
    runs-on: ubuntu-latest
    env:
      FTP_SERVER_DEFAULT: ftp.audedev.com
      FTP_USERNAME_DEFAULT: sitebuilder@audedev.com
      FTP_PORT_DEFAULT: '21'
      FTP_PROTOCOL_DEFAULT: ftps
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: |
          if [ -f package-lock.json ]; then
            npm ci
          else
            npm install
          fi

      - name: Build
        env:
          VITE_BASE: '/'
        run: npm run web:build

      - name: Ensure SPA fallback (optional)
        run: cp dist/index.html dist/404.html || true

      - name: Deploy to Hostinger via FTP (FTPS)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.6
        with:
          server: ${{ secrets.HOSTINGER_FTP_SERVER || env.FTP_SERVER_DEFAULT }}
          username: ${{ secrets.HOSTINGER_FTP_USERNAME || env.FTP_USERNAME_DEFAULT }}
          password: ${{ secrets.HOSTINGER_FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: /public_html/
          protocol: ${{ secrets.HOSTINGER_FTP_PROTOCOL || env.FTP_PROTOCOL_DEFAULT }}
          port: ${{ secrets.HOSTINGER_FTP_PORT || env.FTP_PORT_DEFAULT }}
          timeout: 600000
          exclude: |
            **/.git*
            **/.git*/**
            **/node_modules/**
            **/src/**